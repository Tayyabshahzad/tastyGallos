<?php

namespace Database\Seeders;

use App\Models\Refund;
use Illuminate\Database\Seeder;
use Carbon\Carbon;
class RefundSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $refund = new Refund;
        // $refund->order_id = 1;
        // $refund->franchise_id = 1;
        // $refund->user_id = 5;
        // $refund->date =Carbon::now()->addMinutes(2);
        // $refund->reason = 'Your food is loaded with salt.';
        // $refund->status = 'pending';
        // $refund->save();


        // $refund = new Refund;
        // $refund->order_id = 2;
        // $refund->franchise_id = 2;
        // $refund->user_id = 6;
        // $refund->date =Carbon::now()->addMinutes(5);
        // $refund->reason = 'Your food is burned.';
        // $refund->status = 'pending';
        // $refund->save();


        // $refund = new Refund;
        // $refund->order_id = 3;
        // $refund->franchise_id = 2;
        // $refund->user_id = 7;
        // $refund->date =Carbon::now()->addMinutes(8);
        // $refund->reason = "Your food isn't cooked the way it was described on the menu.";
        // $refund->status = 'pending';
        // $refund->save();
    }
}
